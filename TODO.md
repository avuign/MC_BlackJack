# Pseudo-code for MC BlackJack

1. Build objects in the game : `Card` and `Deck` with `shuffle`
2. Build the `Hand` class with score computation
3. `run_game`with a simple hardcoded strategy 
4. Wrap strategies into callable classes
5. Monte Carlo loop
